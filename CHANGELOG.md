## 1.0.0

- Initial release: Ported the original C mnemonicode project to Dart.
- Provided a clean, idiomatic public API (`encodeBytes`, `decodePhrase`, `encodeHex`, `decodeToHex`).
- Included `mnencode` and `mndecode` CLI tools compatible with the original C binaries.
